# Configuration for the current FastAPI/LangGraph backend.
#
# The old backend used many JWT, database, provider, and routing variables.
# They are not read by the current backend and should not be copied here.

APP_ENV=development
LOG_LEVEL=INFO
